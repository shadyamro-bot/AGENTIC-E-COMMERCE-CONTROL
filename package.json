{
  "name": "aec-railway-unified-flat",
  "version": "1.4.0",
  "private": true,
  "description": "AGENTIC E-COMMERCE CONTROL - Railway flat upload package v1.4.0 stable release with diagnostics, retained source files, reanalysis, corrected exports, hardened capabilities and safer AI workflows",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check parser.js && node --check amazonTemplateParser.js && node parser-v122.test.js && node stable.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "helmet": "^8.1.0",
    "pg": "^8.16.3",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  }
}
